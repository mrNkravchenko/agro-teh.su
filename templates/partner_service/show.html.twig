{% extends 'base.html.twig' %}

{% block title %}PartnerService{% endblock %}

{% block body %}
    <h1>PartnerService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner_service.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner_service.name }}</td>
            </tr>
            <tr>
                <th>Short_name</th>
                <td>{{ partner_service.shortName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partner_service_index') }}">back to list</a>

    <a href="{{ path('partner_service_edit', {'id': partner_service.id}) }}">edit</a>

    {{ include('partner_service/_delete_form.html.twig') }}
{% endblock %}
