{% extends 'base_angar.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    {{ parent() }}
{% endblock %}

{% block title %}Галлерея АГРО-ТЕХ{% endblock %}
{% block h1 %}Галлерея АГРО-ТЕХ{% endblock %}
{% block content %}

<div class="row gallery">
    {#TODO сделать для админа#}
    {#{% if session.role == 3 or session.role == 2 %}
        <div class="col-lg-2 col-md-3 col-sm-4 col-xs-6 thumb hangars__item">

            <form action="/admin/uploadGallery" method="post" enctype="multipart/form-data">
                <div class="field">
                    <div class="alert-warning">
                        <p>последнее фото <span>{{ lastItem.name }}</span></p>
                    </div>
                    <label>Добавить изображение</label>
                    <input type="file" name="file[]" multiple accept="image/*">
                    #}{#<input type="submit">#}{#
                </div>
                <div class="tooltip" style="display: none;"><b></b><span></span></div>
                <input type="submit" value="Добавить">
            </form>

        </div>
    {% endif %}#}
    {% for item in gallery %}
        <div class="col-lg-2 col-md-3 col-sm-4 col-xs-6 thumb hangars__item">

            <a data-fancybox="gallery" href="{{ item.getPath }}{{ item.getNameMd5 }}">
                <img class="img-responsive" src="{{ item.getPathThumbnail }}{{ item.getNameMd5 }}" alt="">
            </a>
            {#{% if session.role == 3 or session.role == 2 %}
                <a href="/gallery/deleteImage?id={{ items.id }}&name_md5={{ items.name_md5 }}">удалить</a>
            {% endif %}#}

            {#<div class="thumbnail">
                <img src="{{ items.path }}{{ items.name_md5 }}" alt="..." class="img-responsive">
            </div>#}
        </div>
    {% endfor %}

</div>

{% endblock %}









