{% extends 'base.html.twig' %}

{% block title %}AngarImage{% endblock %}

{% block body %}
    <h1>AngarImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ angar_image.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ angar_image.name }}</td>
            </tr>
            <tr>
                <th>Name_md5</th>
                <td>{{ angar_image.nameMd5 }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ angar_image.path }}</td>
            </tr>
            <tr>
                <th>Path_thumbnail</th>
                <td>{{ angar_image.pathThumbnail }}</td>
            </tr>
            <tr>
                <th>First</th>
                <td>{{ angar_image.first ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('angar_image_index') }}">back to list</a>

    <a href="{{ path('angar_image_edit', {'id': angar_image.id}) }}">edit</a>

    {{ include('angar_image/_delete_form.html.twig') }}
{% endblock %}
