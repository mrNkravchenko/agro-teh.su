<form method="post" action="{{ path('angar_image_delete', {'id': angar_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ angar_image.id) }}">
    <button class="btn">Delete</button>
</form>
