{% extends 'base.html.twig' %}

{% block title %}PartnerTechImage index{% endblock %}

{% block body %}
    <h1>PartnerTechImage index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Path</th>
                <th>Is_slider</th>
                <th>Created</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for partner_tech_image in partner_tech_images %}
            <tr>
                <td>{{ partner_tech_image.id }}</td>
                <td>{{ partner_tech_image.path }}</td>
                <td>{{ partner_tech_image.isSlider ? 'Yes' : 'No' }}</td>
                <td>{{ partner_tech_image.created ? partner_tech_image.created|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('partner_tech_image_show', {'id': partner_tech_image.id}) }}">show</a>
                    <a href="{{ path('partner_tech_image_edit', {'id': partner_tech_image.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('partner_tech_image_new') }}">Create new</a>
{% endblock %}
