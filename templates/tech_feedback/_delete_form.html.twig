<form method="post" action="{{ path('tech_feedback_delete', {'id': tech_feedback.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tech_feedback.id) }}">
    <button class="btn">Delete</button>
</form>
