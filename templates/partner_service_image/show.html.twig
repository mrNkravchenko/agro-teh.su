{% extends 'base.html.twig' %}

{% block title %}PartnerServiceImage{% endblock %}

{% block body %}
    <h1>PartnerServiceImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner_service_image.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partner_service_image_index') }}">back to list</a>

    <a href="{{ path('partner_service_image_edit', {'id': partner_service_image.id}) }}">edit</a>

    {{ include('partner_service_image/_delete_form.html.twig') }}
{% endblock %}
