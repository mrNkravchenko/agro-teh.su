<form method="post" action="{{ path('partner_service_image_delete', {'id': partner_service_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partner_service_image.id) }}">
    <button class="btn">Delete</button>
</form>
